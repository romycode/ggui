// Code generated by waygenerator. DO NOT EDIT.

package wlcore

import "fmt"

// Gadget is the client proxy for the wl_gadget interface.
type Gadget struct {
	ProxyBase
	listener GadgetListener
}

var _ Proxy = (*Gadget)(nil)

func newGadget(id, version uint32, conn *Conn) *Gadget {
	return newGadgetFromProxyBase(NewProxyBase(id, version, conn))
}

func newGadgetFromProxyBase(base ProxyBase) *Gadget {
	g := &Gadget{ProxyBase: base}
	return g
}

// SetListener installs the handlers for wl_gadget's events, replacing any
// already installed. A nil field ignores that event; a file descriptor
// arriving in an ignored event is closed, not leaked.
func (g *Gadget) SetListener(l GadgetListener) { g.listener = l }

// GadgetListener holds the handlers for wl_gadget's events. Its zero value
// ignores every event. See [Gadget.SetListener].
type GadgetListener struct {
}

// GadgetInterface describes wl_gadget for [Registry.Bind]: the name it
// carries on the wire and version 3, the highest this binding implements.
// Bind negotiates that against the version the compositor advertises, so a
// call site never repeats either value.
var GadgetInterface = Interface[*Gadget]{
	Name:       "wl_gadget",
	MaxVersion: 3,
	New:        newGadgetFromProxyBase,
}

func (g *Gadget) Release() error {
	if g.Version() < 3 {
		return fmt.Errorf("wlcore: release requires version >= 3, got %d", g.Version())
	}
	err := g.Conn().Send(g.ID(), opReqGadgetRelease, NewEncoder())
	g.Conn().Destroy(g)
	return err
}

// Dispatch decodes one wl_gadget event and calls the matching field of the
// listener. The connection calls it while pumping messages; it is exported
// only because the runtime's Proxy interface requires it.
func (g *Gadget) Dispatch(opcode uint16, dec *Decoder) error {
	switch opcode {
	default:
		return fmt.Errorf("wlcore: unknown opcode %d in wl_gadget", opcode)
	}
}

const (
	opReqGadgetRelease = 0
)
