# Task 3 report: generated keysym tables and F-key names

## Summary

- Generated and committed `keyboard/keysyms.gen.go` from the vendored
  libxkbcommon 1.13.2 header.
- Replaced the handwritten `keysymNames` and `legacyRunes` seeds with the
  generated name, canonical-name, and rune tables.
- Added `Keysym.Name`, retaining `Keysym.Rune() == -1` for F1 and other
  non-printable keysyms.
- Added exact virtual-modifier mask assertions and generated-table behavior
  coverage for F1-F12, ISO_Level3_Latch, legacy scripts, and automatic type
  selection.
- Updated keylog output to give symbolic identity and rune output separate
  fields, and added a committed-output freshness test.
- Commit: `a7531cd keyboard: generate complete keysym tables`.

## Commands and results

- `GOCACHE=/tmp/ggui-keysym-go-cache go run ./cmd/keysymgen`: PASS.
- `test -f keyboard/keysyms.gen.go && head -1 keyboard/keysyms.gen.go`: PASS;
  first line was `// Code generated by keysymgen. DO NOT EDIT.`.
- Focused behavior command:
  `GOCACHE=/tmp/ggui-keysym-go-cache go test ./keyboard ./example/keylog -run 'TestParseGenerated|TestKeysymName|TestGeneratedLegacy|TestGuessTypeUses|TestSymLabel' -count=1 -v`:
  PASS after implementation.
- Full focused command:
  `GOCACHE=/tmp/ggui-keysym-go-cache go test ./cmd/keysymgen/... ./keyboard ./example/keylog -count=1 -v`:
  PASS, including `TestCommittedOutputIsCurrent` and exact vmod masks.
- `GOCACHE=/tmp/ggui-keysym-go-cache go build ./...`: PASS.
- `GOCACHE=/tmp/ggui-keysym-go-cache go vet ./...`: PASS.
- `GOCACHE=/tmp/ggui-keysym-go-cache go test -count=1 ./...`: initial
  sandbox run failed only in `wayland/wlcore` because Unix socket syscalls
  returned `operation not permitted`; rerun with Unix-socket permission:
  PASS for every package.
- `GOCACHE=/tmp/ggui-keysym-go-cache go test -race -short -count=1 ./...`
  with Unix-socket permission: PASS for every package.
- `git diff --check`, `git diff --cached --check`, and targeted `gofmt -l`:
  PASS / no output.
- `golangci-lint` was not installed, so no golangci-lint command was run.

## RED/GREEN evidence

RED, after adding the requested tests and before production changes:

```text
keyboard/xkbmini_test.go:459:23: tt.keysym.Name undefined
example/keylog/window_test.go:10:18: undefined: symLabel
FAIL github.com/romycode/ggui/keyboard [build failed]
FAIL github.com/romycode/ggui/example/keylog [build failed]
```

These were the expected missing APIs, not malformed tests.

The first GREEN attempt passed every new keyboard test but exposed the label
width mismatch:

```text
symLabel(F1) = "F1(0x0000ffbe)", want "F1(0x00ffbe)"
```

After correcting the formatter width, all five focused behavior tests passed.
The full focused, build, vet, full test, and race/short suites then passed.

## Generated-file idempotence

SHA-256 immediately before the second generator run:

```text
977b849a40a72aec724f01ead841b2d8f07d076aad315fd84e8d468756aee651
```

SHA-256 immediately after the second generator run:

```text
977b849a40a72aec724f01ead841b2d8f07d076aad315fd84e8d468756aee651
```

The hashes are identical, proving the second run made no change.

## Files

- Added `keyboard/keysyms.gen.go`.
- Modified `keyboard/xkbmini.go`.
- Modified `keyboard/compose.go`.
- Modified `keyboard/doc.go`.
- Modified `keyboard/xkbmini_test.go`.
- Modified `cmd/keysymgen/main_test.go`.
- Modified `example/keylog/window.go`.
- Added `example/keylog/window_test.go`.

## Self-review

- Formatting/imports: clean under gofmt; generated file carries the required
  generated-code marker.
- API/docs: `Keysym.Name` is documented and located beside `Rune`; ParseKeysym
  and Rune comments describe complete generated tables without referencing
  waygenerator or a handpicked subset.
- Behavior: canonical generated names take priority, direct Unicode names use
  uppercase `U%04X`, other unknowns use zero-padded lowercase hexadecimal, and
  F1 remains non-printable.
- Data/control flow: runtime lookups use the generated maps directly; deliberate
  numeric zero resolution remains unchanged; standard named vmods resolve to
  exact Mod5/Mod2 masks.
- Logging: retained the example's existing `log.Printf` architecture as
  required, changing only the output fields and adding the name/value label.
- Tests: assertions exercise real runtime and generator behavior with literal
  expectations; freshness compares regenerated bytes against committed bytes.
- Security/concurrency/errors: no new external inputs, goroutines, secrets, or
  error-flow changes. Generator errors remain checked in the freshness test.
- No must-fix, should-fix, or nit findings remain after review.

## Concerns

- The brief's sample `symLabel` implementation used `%#08x`, but Go renders
  that as `0x0000ffbe`, contradicting the required test value `0x00ffbe`.
  The implementation uses `%#06x` so the explicit behavior contract passes.
- Full Wayland transport tests require Unix-socket permission in this sandbox;
  they passed when rerun with that permission.
- `golangci-lint` is unavailable in the environment; gofmt, go vet, full tests,
  and race/short tests were used for mechanical verification.
