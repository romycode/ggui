# Review package: ff66dcc90bae857cfffaba7f1077f8ae2821718c..HEAD

## Commits
d8cb6ef keysymgen: render and emit deterministic Go tables

## Files changed
 cmd/keysymgen/internal/keysymdata/emit.go        | 71 +++++++++++++++++++++++
 cmd/keysymgen/internal/keysymdata/emit_test.go   | 49 ++++++++++++++++
 cmd/keysymgen/internal/keysymdata/render.go      | 73 ++++++++++++++++++++++++
 cmd/keysymgen/internal/keysymdata/render_test.go | 68 ++++++++++++++++++++++
 cmd/keysymgen/main.go                            | 24 ++++++++
 cmd/keysymgen/main_test.go                       | 45 +++++++++++++++
 6 files changed, 330 insertions(+)

## Diff
diff --git a/cmd/keysymgen/internal/keysymdata/emit.go b/cmd/keysymgen/internal/keysymdata/emit.go
new file mode 100644
index 0000000..df0666d
--- /dev/null
+++ b/cmd/keysymgen/internal/keysymdata/emit.go
@@ -0,0 +1,71 @@
+package keysymdata
+
+import (
+	"errors"
+	"fmt"
+	"os"
+	"path/filepath"
+)
+
+// Emit generates Go source from inputPath and atomically writes it to outputPath.
+func Emit(inputPath, outputPath string) (err error) {
+	input, err := os.Open(inputPath)
+	if err != nil {
+		return fmt.Errorf("keysymgen: open input %q: %w", inputPath, err)
+	}
+
+	generated, generateErr := Generate(input)
+	if closeErr := input.Close(); closeErr != nil {
+		closeErr = fmt.Errorf("keysymgen: close input %q: %w", inputPath, closeErr)
+		if generateErr != nil {
+			return errors.Join(generateErr, closeErr)
+		}
+		return closeErr
+	}
+	if generateErr != nil {
+		return generateErr
+	}
+
+	outputDir := filepath.Dir(outputPath)
+	temporary, err := os.CreateTemp(outputDir, ".keysymgen-*")
+	if err != nil {
+		return fmt.Errorf("keysymgen: create temporary output in %q: %w", outputDir, err)
+	}
+	temporaryName := temporary.Name()
+	defer func() {
+		if temporaryName == "" {
+			return
+		}
+		if removeErr := os.Remove(temporaryName); removeErr != nil && !errors.Is(removeErr, os.ErrNotExist) {
+			removeErr = fmt.Errorf("keysymgen: remove temporary output %q: %w", temporaryName, removeErr)
+			if err == nil {
+				err = removeErr
+			} else {
+				err = errors.Join(err, removeErr)
+			}
+		}
+	}()
+
+	if err := temporary.Chmod(0o644); err != nil {
+		return closeTemporary(temporary, temporaryName, fmt.Errorf("keysymgen: set mode on temporary output %q: %w", temporaryName, err))
+	}
+	if _, err := temporary.Write(generated); err != nil {
+		return closeTemporary(temporary, temporaryName, fmt.Errorf("keysymgen: write temporary output %q: %w", temporaryName, err))
+	}
+	if err := temporary.Close(); err != nil {
+		return fmt.Errorf("keysymgen: close temporary output %q: %w", temporaryName, err)
+	}
+	if err := os.Rename(temporaryName, outputPath); err != nil {
+		return fmt.Errorf("keysymgen: rename temporary output %q to %q: %w", temporaryName, outputPath, err)
+	}
+	temporaryName = ""
+
+	return nil
+}
+
+func closeTemporary(temporary *os.File, temporaryName string, cause error) error {
+	if err := temporary.Close(); err != nil {
+		return errors.Join(cause, fmt.Errorf("keysymgen: close temporary output %q: %w", temporaryName, err))
+	}
+	return cause
+}
diff --git a/cmd/keysymgen/internal/keysymdata/emit_test.go b/cmd/keysymgen/internal/keysymdata/emit_test.go
new file mode 100644
index 0000000..cbcd147
--- /dev/null
+++ b/cmd/keysymgen/internal/keysymdata/emit_test.go
@@ -0,0 +1,49 @@
+package keysymdata
+
+import (
+	"bytes"
+	"os"
+	"path/filepath"
+	"testing"
+)
+
+func TestEmitWritesFormattedGeneratedFile(t *testing.T) {
+	dir := t.TempDir()
+	in := filepath.Join(dir, "keysyms.h")
+	out := filepath.Join(dir, "keysyms.gen.go")
+	if err := os.WriteFile(in, []byte(sampleHeader), 0o644); err != nil {
+		t.Fatal(err)
+	}
+	if err := Emit(in, out); err != nil {
+		t.Fatalf("Emit: %v", err)
+	}
+	data, err := os.ReadFile(out)
+	if err != nil {
+		t.Fatal(err)
+	}
+	if !bytes.HasPrefix(data, []byte("// Code generated by keysymgen. DO NOT EDIT.\n")) {
+		t.Fatalf("missing generated header: %s", data)
+	}
+}
+
+func TestEmitLeavesExistingOutputOnParseFailure(t *testing.T) {
+	dir := t.TempDir()
+	in := filepath.Join(dir, "bad.h")
+	out := filepath.Join(dir, "keysyms.gen.go")
+	if err := os.WriteFile(in, []byte("#define XKB_KEY_Bad 0xnope\n"), 0o644); err != nil {
+		t.Fatal(err)
+	}
+	if err := os.WriteFile(out, []byte("keep me\n"), 0o644); err != nil {
+		t.Fatal(err)
+	}
+	if err := Emit(in, out); err == nil {
+		t.Fatal("Emit succeeded with malformed input")
+	}
+	data, err := os.ReadFile(out)
+	if err != nil {
+		t.Fatal(err)
+	}
+	if string(data) != "keep me\n" {
+		t.Fatalf("output changed after failure: %q", data)
+	}
+}
diff --git a/cmd/keysymgen/internal/keysymdata/render.go b/cmd/keysymgen/internal/keysymdata/render.go
new file mode 100644
index 0000000..aead611
--- /dev/null
+++ b/cmd/keysymgen/internal/keysymdata/render.go
@@ -0,0 +1,73 @@
+package keysymdata
+
+import (
+	"bytes"
+	"fmt"
+	"go/format"
+	"io"
+	"sort"
+	"strconv"
+)
+
+// Render produces formatted Go source defining keyboard keysym lookup tables.
+func Render(t Tables) ([]byte, error) {
+	nameKeys := make([]string, 0, len(t.Names))
+	for name := range t.Names {
+		nameKeys = append(nameKeys, name)
+	}
+	sort.Strings(nameKeys)
+
+	canonicalKeys := sortedKeys(t.CanonicalNames)
+	runeKeys := sortedKeys(t.Runes)
+
+	var buffer bytes.Buffer
+	buffer.WriteString("// Code generated by keysymgen. DO NOT EDIT.\n\n")
+	buffer.WriteString("package keyboard\n\n")
+	buffer.WriteString("var keysymNames = map[string]Keysym{\n")
+	for _, name := range nameKeys {
+		fmt.Fprintf(&buffer, "\t%s: %s,\n", strconv.Quote(name), hex(t.Names[name]))
+	}
+	buffer.WriteString("}\n\n")
+
+	buffer.WriteString("var keysymCanonicalNames = map[Keysym]string{\n")
+	for _, keysym := range canonicalKeys {
+		fmt.Fprintf(&buffer, "\t%s: %s,\n", hex(keysym), strconv.Quote(t.CanonicalNames[keysym]))
+	}
+	buffer.WriteString("}\n\n")
+
+	buffer.WriteString("var legacyRunes = map[Keysym]rune{\n")
+	for _, keysym := range runeKeys {
+		fmt.Fprintf(&buffer, "\t%s: %s,\n", hex(keysym), hex(uint32(t.Runes[keysym])))
+	}
+	buffer.WriteString("}\n")
+
+	formatted, err := format.Source(buffer.Bytes())
+	if err != nil {
+		return nil, fmt.Errorf("keysymgen: format generated Go: %w", err)
+	}
+	return formatted, nil
+}
+
+// Generate parses a libxkbcommon keysym header and renders Go source from it.
+func Generate(r io.Reader) ([]byte, error) {
+	tables, err := Parse(r)
+	if err != nil {
+		return nil, err
+	}
+	return Render(tables)
+}
+
+func sortedKeys[V any](values map[uint32]V) []uint32 {
+	keys := make([]uint32, 0, len(values))
+	for keysym := range values {
+		keys = append(keys, keysym)
+	}
+	sort.Slice(keys, func(i, j int) bool {
+		return keys[i] < keys[j]
+	})
+	return keys
+}
+
+func hex(value uint32) string {
+	return fmt.Sprintf("0x%04x", value)
+}
diff --git a/cmd/keysymgen/internal/keysymdata/render_test.go b/cmd/keysymgen/internal/keysymdata/render_test.go
new file mode 100644
index 0000000..94b4fbe
--- /dev/null
+++ b/cmd/keysymgen/internal/keysymdata/render_test.go
@@ -0,0 +1,68 @@
+package keysymdata
+
+import (
+	"bytes"
+	"strings"
+	"testing"
+)
+
+func TestRenderDeterministicGo(t *testing.T) {
+	tables := Tables{
+		Names:          map[string]uint32{"F2": 0xffbf, "F1": 0xffbe},
+		CanonicalNames: map[uint32]string{0xffbf: "F2", 0xffbe: "F1"},
+		Runes:          map[uint32]rune{0x03bc: '\u0167', 0xff8d: '\r'},
+	}
+	want := `// Code generated by keysymgen. DO NOT EDIT.
+
+package keyboard
+
+var keysymNames = map[string]Keysym{
+	"F1": 0xffbe,
+	"F2": 0xffbf,
+}
+
+var keysymCanonicalNames = map[Keysym]string{
+	0xffbe: "F1",
+	0xffbf: "F2",
+}
+
+var legacyRunes = map[Keysym]rune{
+	0x03bc: 0x0167,
+	0xff8d: 0x000d,
+}
+`
+	got, err := Render(tables)
+	if err != nil {
+		t.Fatalf("Render: %v", err)
+	}
+	if string(got) != want {
+		t.Fatalf("Render mismatch:\n--- got ---\n%s\n--- want ---\n%s", got, want)
+	}
+	again, err := Render(tables)
+	if err != nil || !bytes.Equal(got, again) {
+		t.Fatalf("second Render = %q, %v; want identical output", again, err)
+	}
+}
+
+func TestGenerateBuildsAllTables(t *testing.T) {
+	got, err := Generate(strings.NewReader(sampleHeader))
+	if err != nil {
+		t.Fatalf("Generate: %v", err)
+	}
+	for _, declaration := range []string{
+		"var keysymNames = map[string]Keysym{",
+		"var keysymCanonicalNames = map[Keysym]string{",
+		"var legacyRunes = map[Keysym]rune{",
+	} {
+		if !bytes.Contains(got, []byte(declaration)) {
+			t.Errorf("Generate(sampleHeader) missing %q", declaration)
+		}
+	}
+}
+
+func TestGeneratePropagatesParseError(t *testing.T) {
+	_, err := Generate(strings.NewReader("#define XKB_KEY_Bad 0xnope\n"))
+	if err == nil {
+		t.Fatal("Generate(malformed header) succeeded")
+	}
+}
diff --git a/cmd/keysymgen/main.go b/cmd/keysymgen/main.go
new file mode 100644
index 0000000..bbc0a6a
--- /dev/null
+++ b/cmd/keysymgen/main.go
@@ -0,0 +1,24 @@
+package main
+
+import (
+	"fmt"
+	"os"
+
+	"github.com/romycode/ggui/cmd/keysymgen/internal/keysymdata"
+)
+
+const (
+	defaultInput  = "third_party/libxkbcommon/xkbcommon-keysyms.h"
+	defaultOutput = "keyboard/keysyms.gen.go"
+)
+
+func main() {
+	if err := run(defaultInput, defaultOutput); err != nil {
+		fmt.Fprintln(os.Stderr, err)
+		os.Exit(1)
+	}
+}
+
+func run(inputPath, outputPath string) error {
+	return keysymdata.Emit(inputPath, outputPath)
+}
diff --git a/cmd/keysymgen/main_test.go b/cmd/keysymgen/main_test.go
new file mode 100644
index 0000000..95e8c29
--- /dev/null
+++ b/cmd/keysymgen/main_test.go
@@ -0,0 +1,45 @@
+package main
+
+import (
+	"bytes"
+	"os"
+	"path/filepath"
+	"strings"
+	"testing"
+)
+
+const sampleHeader = `
+#define XKB_KEY_NoSymbol 0x000000 /* Special KeySym */
+#define XKB_KEY_KP_Enter 0xff8d /*<U+000D CARRIAGE RETURN>*/
+#define XKB_KEY_F1 0xffbe
+`
+
+func TestRunGeneratesFile(t *testing.T) {
+	dir := t.TempDir()
+	in := filepath.Join(dir, "keysyms.h")
+	out := filepath.Join(dir, "keysyms.gen.go")
+	if err := os.WriteFile(in, []byte(sampleHeader), 0o644); err != nil {
+		t.Fatal(err)
+	}
+	if err := run(in, out); err != nil {
+		t.Fatalf("run: %v", err)
+	}
+	data, err := os.ReadFile(out)
+	if err != nil {
+		t.Fatal(err)
+	}
+	if !bytes.Contains(data, []byte("var keysymNames = map[string]Keysym{")) {
+		t.Fatalf("run(%q, %q) output missing keysym table", in, out)
+	}
+}
+
+func TestRunMissingInputIncludesPath(t *testing.T) {
+	missingPath := filepath.Join(t.TempDir(), "missing.h")
+	err := run(missingPath, filepath.Join(t.TempDir(), "keysyms.gen.go"))
+	if err == nil {
+		t.Fatal("run with missing input succeeded")
+	}
+	if !strings.Contains(err.Error(), missingPath) {
+		t.Errorf("run(%q, _) error = %v, want path included", missingPath, err)
+	}
+}
